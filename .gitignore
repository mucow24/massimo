node_modules/
dist/
.vite/
*.log
.DS_Store

# TypeScript build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Vitest coverage output
coverage/

# Playwright artifacts
test-results/
playwright-report/
.playwright/

# Source archive for fonts (extracted into public/fonts/)
helvetica-neue-*.zip

# Station-drag perf scratch: benchmark harnesses, the map they run against, and
# their Playwright config. All under .perf/ — deliberately NOT inside src/,
# where an ignore rule would silently untrack a future real module.
.perf/
